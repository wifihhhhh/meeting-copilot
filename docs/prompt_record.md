# Prompt 记录

## 结构化提取 Prompt

目标：让模型严格输出 JSON，避免自然语言解释。

关键策略：

- 给出明确 JSON Schema。
- 要求缺失信息使用空字符串或空数组。
- 强调 Action Items 必须包含负责人、任务、截止时间和状态。

## RAG Prompt

目标：限制模型只基于历史会议上下文回答。

关键策略：

- 没有答案时必须说明未找到。
- 回答末尾附来源会议标题。
